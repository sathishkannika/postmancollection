{
  "name": "jsonplaceholder",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "api-tests-production": "newman run Jsonplaceholder.postman_collection.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "sathish",
  
  "dependencies": {
    "newman": "^3.9.4"
  }
}
